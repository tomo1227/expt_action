name: Test

on:
  workflow_dispatch:
    inputs:
    #   zone:
    #     type: choice
    #     description: Zone
    #     options:
    #       - aaa
    #       - bbb
      zone:
        type: environment
run-name: Test for ${{ github.event.inputs.zone }}

jobs:
  environment: ${{ github.event.inputs.zone }}
  run-test:
    runs-on: ubuntu-latest
    env:
      FOO=${{ secrets.FOO }}
    steps:
      - name: Run env
        run: |
          echo "${{ env.FOO }}"
