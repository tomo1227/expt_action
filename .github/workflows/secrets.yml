name: Test

on:
  workflow_dispatch:
    inputs:
    #   zone:
    #     type: choice
    #     description: Zone
    #     options:
    #       - aaa
    #       - bbb
      zone:
        type: environment
run-name: Test for ${{ github.event.inputs.zone }}
# environment: ${{ github.event.inputs.zone }}

env:
  FOO=${{ secrets.FOO }}
jobs:
  run-test:
    runs-on: ubuntu-latest
    steps:
      - name: Run env
        run: |
          echo "${{ env.FOO }}"
